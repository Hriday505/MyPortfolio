"use client";

import Nav from "@/app/Components/Nav";
import { motion } from "framer-motion";
const ProjectData = [
  {
    id: 1,
    title: "Project 1",
    description:
      "If you want, tell me which section should be slightly bigger (Recent/Popular/Live), and I’ll set different widths per section while still keeping “2 visible on mobile.",
    link: "www.website.com",
  },
  {
    id: 2,
    title: "Project 2",
    description:
      "If you want, tell me which section should be slightly bigger (Recent/Popular/Live), and I’ll set different widths per section while still keeping “2 visible on mobile.",
    link: "www.website.com",
  },
  {
    id: 3,
    title: "Project 3",
    description:
      "If you want, tell me which section should be slightly bigger (Recent/Popular/Live), and I’ll set different widths per section while still keeping “2 visible on mobile.",
    link: "www.website.com",
  },
  {
    id: 4,
    title: "Project 4",
    description:
      "If you want, tell me which section should be slightly bigger (Recent/Popular/Live), and I’ll set different widths per section while still keeping “2 visible on mobile.",
    link: "www.website.com",
  },
];

const containerVariants = {
  hidden: { opacity: 0 },
  show: {
    opacity: 1,
    transition: {
      staggerChildren: 0.18,
      ease: "easeOut",
    },
  },
};

const cardFromRightVariants = {
  hidden: { opacity: 0, x: 50 },
  show: {
    opacity: 1,
    x: 0,
    transition: { duration: 0.6, ease: "easeOut" },
  },
};

const cardFromLeftVariants = {
  hidden: { opacity: 0, x: -50 },
  show: {
    opacity: 1,
    x: 0,
    transition: { duration: 0.6, ease: "easeOut" },
  },
};

const titleVariants = {
  hidden: { opacity: 0, y: -20 },
  show: {
    opacity: 1,
    y: 0,
    transition: {
      duration: 0.8,
      ease: "easeOut",
    },
  },
};

const descriptionVariants = {
  hidden: { opacity: 0, y: 30 },
  show: {
    opacity: 1,
    y: 0,
    transition: {
      duration: 1,
      ease: "easeOut",
      delay: 0.1,
    },
  },
};

const buttonVariants = {
  hidden: { opacity: 0, scale: 0.8, y: 10 },
  show: {
    opacity: 1,
    scale: 1,
    y: 0,
    transition: {
      duration: 0.9,
      ease: "easeOut",
      delay: 0.25,
    },
  },
};

export default function ProjectPage({ isdark, setIsdark }) {

  return (
    <>
      <Nav isdark={isdark} setIsdark={setIsdark} />

      <div className="w-full min-h-screen ">
        <div className="max-w-5xl  mx-auto px-4 py-4 sm:px-6 sm:py-8 lg:px-8 lg:py-10">
          <motion.div
            className="grid grid-cols-1 gap-3 sm:gap-4 sm:grid-cols-2 lg:grid-cols-3"
            variants={containerVariants}
            initial="hidden"
            animate="show"
          >
            {ProjectData.map((project, index) => {
              const directionVariants =
                index % 2 === 0 ? cardFromRightVariants : cardFromLeftVariants;

              return (
                <motion.div
                  key={project.id}
                  className="flex flex-col justify-between gap-3 rounded-lg bg-gray-400/80 px-4 py-5 sm:px-5 sm:py-6 shadow-sm max-w-sm w-full mx-auto min-h-56 sm:min-h-52 transform transition-transform duration-300 ease-out hover:scale-[1.02]"
                  variants={directionVariants}
                >
                  <div className="space-y-1.5 ">
                    <motion.h2
                      className="text-base sm:text-lg lg:text-xl font-extrabold text-center sm:text-left"
                      variants={titleVariants}
                    >
                      {project.title}
                    </motion.h2>
                    <motion.p
                      className="text-[15px] sm:text-sm md:text-[14px] mt-2 font-extralight"
                      variants={descriptionVariants}
                    >
                      {project.description}
                    </motion.p>
                  </div>
                  <motion.a
                    href={project.link}
                    className="inline-flex mt-4 w-32 h-9 items-center justify-center rounded-3xl bg-red-50 px-4 py-2 font-medium text-black hover:bg-red-100 transition-colors"
                    style={{ fontSize: "0.875rem" }} // 14px
                    variants={buttonVariants}
                  >
                    View Project
                  </motion.a>
                </motion.div>
              );
            })}
          </motion.div>
        </div>
      </div>
    </>
  );
}

